describe( "Snitcher", function () {

    // TODO
    // We can't test from within mocha, otherwise it will capture, don't know
    // i guess. So tests should remain inside timer and async tickers.

    it( "should work from within a process.nextTick" );
    it( "should work from within setImmediate" );
    it( "should work from within setTimeout" );

});
